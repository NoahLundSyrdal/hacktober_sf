The app offers one API interface: `/login`, which accepts a POST request with a `LoginRequest` object containing two fields: `username` (validated to be 1-64 characters, containing only A-Za-z0-9._- ) and `password` (validated to be 1-128 characters). Only one valid credential pair is accepted.